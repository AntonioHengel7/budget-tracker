# TODO

## Phase A — Scaffolding (enforcement OFF) — DONE 2026-08-12

- [x] Install Node, scaffold repo, copy harness config
- [x] Project config files (package.json, vite.config.ts, tsconfig.json, .gitignore)
- [x] npm install and verify toolchain
- [x] Seed money.ts domain module + test (100% real coverage, no padding)
- [x] CI workflow — green on first push (github.com/AntonioHengel7/budget-tracker)
- [x] Phase A done checklist (see plan) — all 7 items verified, including negative cases
- [x] Create GitHub repo, push, create labels (11/11)

## Phase B — Shakedown (enforcement ON)

- [x] B0: flip enforce:true, branch protection, dress-rehearsal PR (issue #1) — DONE 2026-08-12, merged as PR #2
- [x] B1: real domain issue — transaction/budget/period model (issue #3) — DONE 2026-08-13, merged as PR #4 after 4 review rounds
- [x] B2: JSON storage layer + real CLI commands (issue #6) — DONE 2026-08-14, merged as PR #7 (`80d4afa`) after 4 review rounds
- [x] File follow-up issues — DONE 2026-08-14: 8 filed on budget-tracker (#8-#15), 7 on Jome (#1-#7). See review section below.
- [x] Decide on a deployable UI layer — DONE 2026-08-16: design (`docs/dev-notes/2026-08-14-web-ui.md`) + plan (`docs/plans/2026-08-14-web-ui.md`, `[Approved]`) written, deploy target Fly.io (kept over Vercel — Vercel's serverless model has no persistent filesystem, would force a storage-layer rewrite away from the reused `jsonStore.ts`), auth is username+password, owner-only at launch.

## Phase C — Web UI + deploy (milestone "Web UI + deploy")

- [x] C1: auth + session foundation (issue #16) — DONE 2026-08-16, merged as PR #20 (`34e16df`) after 3 review rounds (see below)
- [x] C2: Express API layer (issue #17) — DONE 2026-08-17, merged as PR #22 (`c8789c9`) after 2 review rounds (see below). Plan doc §Step 2 error-mapping correction also landed.
- [x] C3: frontend (issue #18) — DONE 2026-08-18, merged as PR #24 (`d898c87`) after 3 review rounds (see below). A post-merge CI break (issue #29) was found and fixed separately as PR #30 (`57c6b3b`) — see below.
- [x] Manual real-browser click-through of the web UI (plan's Step 3 gate for #18) — DONE 2026-08-18, driven live via `claude-in-chrome` against a local server + `web` dev server. Login → add transaction → set a limit all worked correctly and matched the network responses exactly (including confirming the round-2 `rollover`-omission fix still holds: the PUT body had no `rollover` key at all since the checkbox was never touched). **Confirming the status step surfaced a real bug, filed as #31 (p1)**: `budgetStatus()`'s `availableMinor` (`src/domain/budget.ts:192`) is the period's spending ceiling (`limit + carry`), correctly used internally for `state`/`pctUsed`, but returned to both the CLI (`status` command) and the web dashboard under the name "available" — read by both as "remaining to spend," a different quantity nothing computes. Reproduced live: a 100.00 limit with 25.00 spent showed "Available: 100.00" instead of 75.00. Not fixed inline — filed for the normal issue → builder → review pipeline. Also noted, cosmetic only: the UI is fully unstyled (no CSS), consistent with this being an MVP frontend; not filed since it's a known, accepted state, not a discovered defect.
- [ ] C4: Docker + Fly.io deploy (issue #19) — unblocked (#18 merged). User creating Fly.io account. **Carry-forward condition from Hobbes (C2 round 2): `fly.toml`/deploy config must explicitly pin `TRUST_PROXY` — the shipped default of 1 is correct behind Fly's single-hop edge but silently wrong (rate-limit bypass via spoofed `X-Forwarded-For`) on any other topology. Hobbes will block the deploy PR if this isn't explicit.** Worth deciding whether #31 (available-to-spend math) should block deploy or ship alongside it — it's a real user-facing correctness bug, not cosmetic.

## Review — C3 frontend (2026-08-17/18)

Issue #18, PR #24, 3 review rounds before merging clean; a post-merge CI break fixed as a separate follow-up (issue #29, PR #30):
- Round 1: Plato PASS, Hobbes PASS (confirmed CSP/helmet NOTEs from #17 correctly stay deferred to #19 since `staticDir` is dead code until then; no XSS sink; clean `npm audit`). **Socrates FAIL — 5 blocking**: `Limits.tsx` always sent `rollover` in the PUT body, silently flipping an existing `true` to `false` on any edit — real data corruption, not just UX; `Login.tsx`'s deviation from `api.ts` was justified by a false claim about the frozen test's `{ok:false}` mock (`parseErrorMessage` already try/catches `res.json()`), costing an unhandled promise rejection on network failure and a 429 rate-limit misreported as "invalid credentials"; `App.tsx`'s `handleLogout` was unguarded, silent no-op on failure.
- Fix (`2ae354a`): `Limits.tsx` only sends `rollover` if the user touched the checkbox; `Login.tsx` routes through `api.ts`'s `login()` with proper try/catch, distinguishes 429; `App.tsx` logout resolves to logged-out UI via try/finally; added `web/tests/Login.errors.test.tsx`.
- Round 2 at `2ae354a` (fresh verdicts required — Plato/Hobbes's round-1 PASS was pre-fix): Hobbes PASS. **Socrates FAIL — 3 blocking**: the logout fix's own `try/finally` re-threw past the catch, reintroducing an unhandled rejection at the `void handleLogout()` call site; `rolloverTouched` was never reset, so a second submit for a different category in the same mount leaked the first category's touched-rollover value; zero regression tests existed for either round-1 fix and CI never ran `web/`'s suite at all (confirmed by reverting both fixes and getting a fully green suite). **Plato FAIL — 2 blocking**: `Login.errors.test.tsx` used a describe-level comment instead of this repo's per-`it` citation convention; `Transactions.tsx:34`'s dead `eslint-disable` was a second independent occurrence of the pattern first fixed in #17.
- Fix (`0792029`, builder agent): logout now catches before finally; `Limits.tsx` resets `rollover`/`rolloverTouched` on category change and post-submit; added `web/tests/App.logout.test.tsx` and `web/tests/Limits.rollover.test.tsx` (both confirmed by mutation-revert to actually catch their bugs); added a `web` job to `.github/workflows/ci.yml` running install/typecheck/test/build; citations fixed; dead comment removed, follow-up filed as #25.
- Round 3 at `0792029`: **Socrates PASS, Plato PASS, Hobbes PASS** (fresh Hobbes check on the round-3 delta, since its round-2 PASS was pre-fix — clean, only CI-hardening NOTEs: missing `permissions: contents: read`, unpinned action tags). All four fixes verified by mutation-revert. Merged via `gh pr merge 24 --squash --delete-branch --match-head-commit 0792029` → `d898c87`, issue #18 auto-closed.
- Non-blocking follow-ups filed: #25 (recurring dead eslint-disable pattern), #26 (rollover regression test only catches the paired revert, not either reset alone), #27 (`web/` has no coverage-gate enforcement, unlike root's `src/domain/**`).
- **Post-merge CI break (issue #29)**: the newly-added `web` CI job (from the round-2 fix above) ran on PR #24 at the merge head `0792029` and completed **red** at 00:30:42Z — `web`'s `npm run typecheck` failed (`Cannot find name 'process'` in `web/tests/App.logout.test.tsx`, missing `@types/node`, which CI's isolated `web` job couldn't fall back to root's copy of). All three round-3 PASS verdicts were posted ~10 minutes *after* that red result was already visible on the PR, and the merge went through ~12 minutes after it, so `main` landed red. Nobody — not the three reviewers, not the gate — read `statusCheckRollup` before passing/merging; `pre-merge-gate.sh` only checks posted verdict comments and never checks GitHub's actual CI status (filed as Jome#9, a real gap in the harness, not budget-tracker-specific — the rollup data existed and was queryable, this wasn't a timing gap). Caught by Socrates during review of an unrelated follow-up docs PR, re-reading #24's own history against live CI state. Fixed as PR #30 (`e3d63db`): added `@types/node` as a `web/` devDependency. Reviewed clean (Socrates/Plato/Hobbes all PASS, this time with an explicit live-CI-status check per reviewer, not just local reproduction) and merged → `57c6b3b`. `main`'s CI confirmed green after merge.
- Also noted: this whole review cycle's dispatches (rounds 2 and 3, plus PR #30's review) all raced on budget-tracker's one shared checkout despite requesting `isolation: "worktree"` on each agent — the flag only isolates relative to the orchestrator's own repo (Jome), not a different repo an agent `cd`s into. No damage since every reviewer verified by execution/mutation rather than trusting file state, but logged as a Jome lesson (promoted to long-term 2026-08-17).

## Review — C1 auth + session foundation (2026-08-16)

Issue #16, PR #20, 3 review rounds before merging clean:
- Round 1: Plato PASS, Hobbes PASS (empty-secret forgery closed, timing side-channel not measurable, path-traversal charset airtight, supply chain clean — but flagged 3 non-blocking footguns worth fixing before #17: empty-string `SESSION_SECRET` accepted, username-enumeration timing via `findCredential` early-return, `verifyPassword` throwing on non-string input). **Socrates FAIL — 1 blocking**: usernames containing `.` broke session token parsing (delimiter collision — `session.ts` splits tokens on `.`), and no shared username-validation contract existed across `session.ts`/`credentials.ts`/`paths.ts`.
- Fix (`1bf56ec`): `paths.ts` now exports canonical `isValidUsername`/`assertValidUsername`; `loadCredentials` validates every username at config-load time, closing the delimiter collision at the boundary; `InvalidUsernameError` now extends `DomainError`. Also bundled Hobbes's 3 items: empty-secret rejected, constant-time-ish `authenticate()` added, non-string password no longer crashes. Plus Plato's 2 NOTEs (readonly fields, error-logging consistency).
- Round 2 at `1bf56ec`: Socrates PASS (blocking genuinely closed, reproduced by execution), Plato PASS. **Hobbes PASS but flagged a new issue**: the constant-time guarantee was real but unenforced — `DUMMY_HASH` hardcoded at bcrypt cost 10 with no validation that configured hashes matched; a cost-12 hash reopened a 193ms timing oracle, and a malformed hash (already exercised by the frozen test fixture) inverted it. Socrates independently corroborated the same finding and additionally flagged `loadCredentials` throwing two unrelated error types (`CredentialsConfigError` vs `InvalidUsernameError`) for "malformed config."
- Fix (`5a4f5b9`): pinned `BCRYPT_COST` constant, `DUMMY_HASH` derived from it, `loadCredentials` rejects any hash with a wrong cost or malformed shape at boot; `InvalidUsernameError` now wrapped as `CredentialsConfigError` for a single catchable error type. One frozen-test fixture (`'$2a$...'` placeholder, never a real hash) legitimately updated to a real generated hash — flagged, not silent.
- Round 3 (final, tight re-checks) at `5a4f5b9`: **Socrates PASS, Plato PASS, Hobbes PASS** — all reproduced the fixes by execution (fed real cost 4-14 hashes through the actual load→authenticate path), confirmed the oracle can no longer occur. Merged via `gh pr merge 20 --squash --match-head-commit 5a4f5b9...` → `34e16df`.
- Carried forward to #17 (not reachable until a server exists, must be addressed there): `SESSION_SECRET` guard only rejects empty string, not short/weak secrets; `verifySession` throws on `undefined` secret contradicting its own "Never throws" docstring — needs boot-time validation in `src/server/index.ts`, not per-request handling; `findCredential`/`verifyPassword` remain exported for the frozen tests but must not be used directly for a real login check (use `authenticate()`).
- Follow-up issue filed: #21 (`hashPassword.ts` hardcoded cost literal instead of importing `BCRYPT_COST`, non-canonical token encoding, duplicate-username first-wins, zero-account boot allowed, `hashPassword.ts` untested) — all non-blocking, `priority:p2`.

## Review — C2 Express API layer (2026-08-16/17)

Issue #17, PR #22, 2 review rounds before merging clean:
- Round 1 at `a5f9ecc`: Plato PASS (cosmetic NOTEs only — non-nested-readonly array type, dead `eslint-disable` comments referencing a linter this repo doesn't have, blind `as string` casts on `req.username`/params). **Hobbes FAIL — 1 blocking**: no rate limiting anywhere on `/api/login` — measured live, 8 concurrent connections from one attacker degraded `/healthz` latency 1379x (0.37ms → 508ms) and allowed ~16 unthrottled login attempts/sec, a real DoS + credential-brute-force vector on the planned single-container Fly deploy. **Socrates FAIL — 1 blocking**: `StorageError` responses mapped to 400 with the raw message, reproduced leaking absolute filesystem paths and OS error text to clients; malformed JSON bodies on unauthenticated routes crashed to 500 with a full stack trace instead of a clean 400. Socrates noted this was literally what the plan itself pinned (not a builder deviation), requiring a plan-doc correction alongside the code fix.
- Fix (`a80eaca`): `express-rate-limit` added on `POST /api/login` (configurable via `AppConfig.loginRateLimit`), `app.set('trust proxy', config.trustProxy ?? 0)` wired through `AppConfig`/`TRUST_PROXY` env (validated in `boot()`). Error mapping corrected: `DomainError`→400 with message, `StorageError`→500 generic body (real detail logged server-side only), body-parser `SyntaxError`→400. Also bundled: cookies now default `Secure: true` with explicit opt-out (`insecureCookies`/`INSECURE_COOKIES`) instead of depending on an unset `NODE_ENV`; `PORT` added to the boot-time fail-fast validation; blind `as string` casts replaced with `AuthenticatedRequest`/`isAuthenticatedRequest` runtime narrowing + a single `authed()` wrapper; dead eslint-disable comments removed.
- Round 2 at `a80eaca`: **Plato PASS** (frozen `api.integration.test.ts` diff confirmed pure-addition — only `insecureCookies: true` added to existing calls plus 3 new appended tests, no frozen assertion touched; new `tests/server/index.test.ts`, 11 tests, confirmed meaningful). **Socrates PASS** — reproduced by real execution against a booted server: corrupted store files / `chmod 000` / directory-in-place-of-file / read-only data dir all now return generic 500s with detail server-side only; malformed JSON now 400 with no stack trace; re-attacked tenant isolation after the `authed()` refactor, still airtight. **Hobbes PASS** — repeated the exact 8-connection attack: 181,665 requests in 8s, only 10 reached bcrypt, 181,655 got 429s, `/healthz` showed zero measurable degradation (vs 1379x in round 1); confirmed `Secure` cookie now ships by default and the opt-out fails closed; re-ran full tenant-isolation spoof battery, clean; `npm audit` clean on the new `express-rate-limit` dependency. Merged via `gh pr merge 22 -R AntonioHengel7/budget-tracker --squash --match-head-commit a80eaca...` → `c8789c9`.
- Plan-doc correction: `docs/plans/2026-08-14-web-ui.md` §Step 2 amended 2026-08-17 to match the corrected error-mapping code (was: `DomainError`/`StorageError` both →400 with raw message; now: `DomainError`→400 with message, `StorageError`→500 generic + server-side-only logging, malformed-JSON→400).
- Carried forward, non-blocking:
  - **To #19 (deploy), hard condition from Hobbes**: `TRUST_PROXY` default of 1 is correct behind Fly's single-hop edge but silently wrong (rate-limit bypass via spoofed `X-Forwarded-For`) on any other topology — the deploy PR must pin it explicitly in `fly.toml`/env, or Hobbes will block it.
  - **To #18 (frontend)**: no `helmet()`, `X-Powered-By: Express` still on the wire — not blocking today since no HTML is served yet, but Hobbes flagged this becomes blocking once `staticDir` starts serving the SPA (CSP is the main XSS defense-in-depth layer for a frontend that lands next).
  - Untested behavior worth covering: `Secure`-by-default and `X-Forwarded-For`-keying have no automated test — the round-1 bypass would have passed CI silently.
  - Minor, unfixed: non-`entity.parse.failed` body-parser errors (oversized body, bad `content-encoding`) still 500 before the login limiter runs; `TRUST_PROXY` default (1) contradicts `AppConfig`'s own doc comment (implies 0/explicit-only) — Hobbes judged the default itself correct for the pinned Fly topology, just under-documented; entrypoint guard silently no-ops through a symlink; `PORT` accepts hex/exponential-notation strings via `Number()` coercion; `CredentialsConfigError` still flattened to a plain `Error` crossing `boot()`; logout still can't revoke a token (inherent to stateless design, consciously accepted); repeated query params (`?from=a&from=b`) silently dropped instead of rejected.

## Review — B2 storage + CLI (2026-08-14)

Issue #6, PR #7, 4 review rounds before merging clean:
- Round 1: Plato PASS, Hobbes PASS (exploit suite: symlink attacks, prototype pollution, 22-case tamper matrix, ReDoS, concurrent-write races — nothing crossed a real boundary). **Socrates FAIL — 2 blocking**: `limit set` on an already-covered period silently no-op'd instead of replacing; `loadStore` didn't genuinely type-check persisted fields before handing them to domain constructors.
- Fix (`110c1f4`): `limit set` replaces an existing limit at the same `effectiveFrom`; `loadStore` runs explicit `assertFieldType` checks before every domain-constructor call; bundled `{ flag: 'wx' }` fix for a symlink-following gap in the atomic write.
- Round 2 at `110c1f4`: Plato PASS, Hobbes PASS. **Socrates FAIL — 1 blocking**: the round-1 fix itself introduced a regression — `src/cli/index.ts`'s `limit set` confirmation message printed `budget.limits[length - 1]` (array tail), which stopped being "the limit just set" once `limit set` started replacing in place. Reproduced end-to-end (correcting an earlier period printed the later period's amount; on-disk data was correct, only the message lied).
- Fix (`5f2bf22`, builder agent): confirmation message now looks up the limit matching `options.effectiveFrom` via `.find()` instead of trusting array position.
- Round 3 at `5f2bf22`: **Socrates PASS** (mutation-tested the fix — reverting it reproduces the exact round-2 symptom; grepped for the same "assumes append-only/positional" defect class elsewhere in `src/` — none found; re-verified round-1 fixes hold by mutation, not just by trust; 9 non-blocking NOTEs filed). **Hobbes PASS** (full re-audit: symlink/prototype-pollution/type-confusion protections re-verified empirically against the built artifact, new `.find()` lookup confirmed non-exploitable, supply chain clean). **Plato FAIL — 1 blocking**: the new regression test in `tests/cli/smoke.test.ts` didn't follow this repo's `// Regression (Reviewer, PR #N ...)` citation-comment convention that all other regression tests use.
- Fix (`c9a443c`): added the citation comment, comment-only diff, verified by all three reviewers.
- Round 4 at `c9a443c`: **Socrates PASS, Plato PASS, Hobbes PASS** — all confirmed the delta was genuinely comment-only via `git diff`, no re-review needed of already-covered logic. Merged via `gh pr merge 7 -R AntonioHengel7/budget-tracker --squash --match-head-commit c9a443c...` → `80d4afa`.
- Post-merge: pulled `main`, ran a real end-to-end manual smoke test against the built CLI (`node dist/cli/index.js`) — add/list/rm/limit-set/status/summary all correct, and the exact round-2 bug scenario (correct an earlier period after setting a later one) now prints the right amount in the live build, not just in tests.
- Operational note: `pre-merge-gate.sh` requires the FULL 40-char commit SHA on the verdict's first line, not the short form — all three reviewer agents initially posted short SHAs (`PASS @ c9a443c`) and had to repost. Worth fixing the reviewer-agent prompts/`gh-schema.md` to make this explicit (added to the Jome follow-up list below).

**Follow-up issues filed 2026-08-14** (all `source:discovered`, from the B1+B2 review cycles):
- **budget-tracker**: #8 duplicate-id handling in `rm`, #9 concurrent read-modify-write data loss, #10 terminal escape-sequence passthrough (`type:security`), #11 missing category/note length bound, #12 empty-string `BUDGET_FILE` fallback, #13 `list --category` not trimming, #14 status row reordering (cosmetic), #15 two budgets sharing a category silently collide on load/save (`priority:p1` — most-repeated finding, flagged independently in both round 2 and round 3).
- **Jome**: #1 `detect_language()` pipefail false-negative, #2 unused `core` globs on the live coverage path, #3 `enable-branch-protection.sh` self-approval-deadlock default, #4 `git-preflight.sh` heredoc/apostrophe quote-stripping, #5 `gh-schema.md` doesn't state the verdict SHA must be full-length (found this round — 3 independent reviewer agents posted short SHAs), #6 `pre-merge-gate.sh`'s `gh pr view` not respecting target-repo cwd, #7 ratchet-baseline field/docs mismatch.

## Review

**B1 (2026-08-13):** Domain layer (date/period/transaction/budget/summary/filter) built by the `builder` subagent, went through 4 real Socrates/Plato/Hobbes review rounds before merging clean:
- Round 1: Socrates PASS, Plato PASS, **Hobbes FAIL — 3 blocking** (`periodsBetween` infinite loop/OOM at year 9999 rollover; `createTransaction` accepted `amountMinor: 1e300`, weaker than `money.ts`'s guard; `budget.ts` had no validating constructor, NaN limit silently reported `state:'under'`, carryover math bypassed overflow protection).
- Round 2 (after fix): **Plato FAIL — 1 blocking** (a `// coverage-skip:` comment invented for an "unreachable" branch didn't match this repo's real enforced skip vocabulary).
- Round 3 (after simplifying carryover per Plato's note): **Socrates FAIL — 2 blocking**, both with concrete reproducers (the "unreachable" cast was actually reachable via a non-zero-padded period literal bypassing `parsePeriod`; a raw `+` at `budgetStatus` could silently exceed `MAX_SAFE_INTEGER`).
- Round 4: **all three PASS**, verified independently (Socrates did mutation testing — reverted the source and confirmed the new regression tests fail on old code, pass on new; Hobbes ran a 3000-case fuzz against a BigInt oracle on the sign-handling arithmetic).

Every blocking finding was real, execution-confirmed, and caught something a "looks right" read wouldn't have. This is the harness's review gate working as designed, not theater.

Also mid-flight: discovered a shared-git-checkout hazard — running multiple Bash-capable review agents against the same working directory (no worktree isolation) let one agent's `git checkout`/cleanup step switch the branch under the others, and later caused a real false-FAIL when one agent's file mutations raced another's test run. Worked around each time (isolated `git archive` exports, explicit `-R owner/repo` on cross-repo `gh` calls); should use `isolation: "worktree"` for this pattern going forward.
